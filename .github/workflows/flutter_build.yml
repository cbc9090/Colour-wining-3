name: colour_winning
description: Colour Prediction Trading App
publish_to: 'none'        # we are not publishing this package
version: 1.0.0+1

environment:
  sdk: ">=2.19.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2      # 👉 उदाहरण dependency

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/images/
